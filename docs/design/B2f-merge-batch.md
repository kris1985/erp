# B2f 设计：合批 L1+L2

> **状态：** ✅ 走查通过（2026-08-09）  
> **总纲：** [`product-roadmap.md`](../product-roadmap.md) §4.2 / §7 B2f  
> **对象：** 合批头挂多张 **生产单** `orders`；账本仍认各 `orders.id`  
> **产品边界与入口演进：** [`merge-batch-aps-alignment.md`](./merge-batch-aps-alignment.md) §10～§12

---

## 1. 目标与边界

**目标**  
勾选同款（默认同色）多张生产单 → 组成合批 → 成员列表 + 汇总色码 → 打印合批流转卡 → 可移出成员并重打。

**不做（§4.2）**

- 批次级领料 / 齐套互斥 / 出货  
- 默认报工自动分摊  
- 真合单（B3c）  
- 排产按批筛选（可选，本轮未做 → 近端补，见下节）

---

## 2. 主路径（现网）

1. `/admin/orders` 勾选 ≥2 张 →「组成合批」  
2. 默认同色校验；失败可改「允许多色」  
3. 弹窗看成员 + 汇总色码 →「打印合批卡」→ `/admin/merge-batches/print/:id`  
4. 可「移出」成员后重打  

---

## 2.1 入口演进（产品已定，实现待排）

| 入口 | 角色 | 职责 |
|------|------|------|
| **排产池（计划主入口）** | PMC | 合批推荐、交期窗、采纳组批、按批筛 → 出排产方案 |
| **生产订单（执行辅入口）** | 跟单/车间 | 保留组批、合批管理、打印/补打卡、移出成员 |
| 智能推荐 | — | **不堆在订单列表**；链到排产或仅在排产池提供 |

现网仅订单入口；近端加排产池能力时**勿删除**订单侧组批/打卡。

---

## 3. 走查证据（2026-08-09）

| 项 | 样例 | 结果 |
|----|------|------|
| ① 不同款硬拦 | A款 + OP-WINTER-09 | 400「不同款无法组成合批」 |
| ② 三单组批 + 汇总 | 230711 / 260701 / B1C-WALK → `MB-260809-002` | 3 单 / 1980 双；色码合计 = 成员 items 之和 |
| ③ 卡字段 | 合批号 + 成员单号/客户/交期 + 汇总表 | DTO 齐全；打印路由 200 |
| ④ 账本仍分单 | 成员 `230711` | work-logs / stock-issues / shipments 仍按 `order_id` 200 |
| ⑤ 移出重打 | 移出 B1C-WALK | 剩 2 单 / 1800 双；详情可再打 |
| 单测 | `test_b2f_merge_batch.py` | 3 passed（含默认同色拦、异款拦） |

---

## 4. 数据 / API

见落地正文：`merge_batches` + `merge_batch_members`；`POST/GET/DELETE` 成员维护。

---

## 5. 修订

| 日期 | 说明 |
|------|------|
| 2026-08-09 | 落地 L1+L2（组批+卡；账本仍分单） |
| 2026-08-09 | **走查通过** |
| 2026-08-09 | 补 §2.1 入口演进：计划主入口→排产池，订单保留执行辅入口 |
